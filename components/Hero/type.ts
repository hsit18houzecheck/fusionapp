// Keep SelectOption for BookingForm and form controls
export type SelectOption = {
  value: string;
  label: string;
};

export type HomeHeroContent = {
  title: string;
  subtitle: string;
  button: {
    label: string;
    url: string;
  };
  ricsLogo: string;
};
