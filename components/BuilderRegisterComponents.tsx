"use client";

// This component imports the registry to register all custom components globally
// It runs on the client side when the app mounts
import "../builder-registry";

export default function BuilderRegisterComponents() {
  return null;
}
